---
title: BallSocketConstraint
description: Creates a ball-and-socket joint between two attachments.
---

## Properties [#properties]

| Property | Type | Default |
| --- | --- | --- |
| `Attachment0` | `any` | `nil` |
| `Attachment1` | `any` | `nil` |
| `Enabled` | `boolean` | `true` |
| `Visible` | `boolean` | `true` |
| `Color` | `any` | `Color3.fromRGB(255, 255, 0)` |
| `Radius` | `number` | `0.1` |
| `Name` | `string` | `"BallSocketConstraint"` |

## Functions [#functions]

<Cards>

  <Card title="`cleanup`" href="#cleanup" description="Cleans up the resources." />
  <Card title="`rebuild`" href="#rebuild" description="Rebuilds the state." />
  <Card title="`onPartChanged`" href="#onpartchanged" description="On part changed." />
  <Card title="`drawGizmo`" href="#drawgizmo" description="Draws the visual representation." />

</Cards>

---

### `cleanup` [#cleanup]

Cleans up the resources.

**Arguments**

<Callout type="info">This function takes no arguments.</Callout>

**Returns**

<Callout type="info">This function returns nothing.</Callout>

---

### `rebuild` [#rebuild]

Rebuilds the state.

**Arguments**

<Callout type="info">This function takes no arguments.</Callout>

**Returns**

<Callout type="info">This function returns nothing.</Callout>

---

### `onPartChanged` [#onpartchanged]

On part changed.

**Arguments**

```luau
prop: any
```

**Returns**

<Callout type="info">This function returns nothing.</Callout>

---

### `drawGizmo` [#drawgizmo]

Draws the visual representation.

**Arguments**

<Callout type="info">This function takes no arguments.</Callout>

**Returns**

<Callout type="info">This function returns nothing.</Callout>

## API

```luau
instance:cleanup()
instance:rebuild()
instance:onPartChanged(prop: any)
instance:drawGizmo()
```
